FROM nginx:alpine

# Copy custom nginx configuration with W3C Trace Context header forwarding
COPY docker/nginx.conf /etc/nginx/conf.d/default.conf

# Expose port 80
EXPOSE 80

# Start nginx
CMD ["nginx", "-g", "daemon off;"]

